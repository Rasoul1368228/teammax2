<!DOCTYPE html>
<html lang="fa" dir="rtl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>تیم مکس | طراحی سایت حرفه‌ای</title>

    <!-- ================== SEO Meta ================== -->
    <meta
      name="description"
      content="تیم مکس، ارائه‌دهنده خدمات طراحی سایت حرفه‌ای با تجربه کاربری عالی و سرعت بالا."
    />
    <meta
      name="keywords"
      content="طراحی سایت، سایت شرکتی، سایت فروشگاهی، UI/UX، تیم مکس، توسعه وب، سئو سایت"
    />
    <meta name="author" content="تیم مکس" />
    <link rel="canonical" href="https://teammax.ir/" />

    <!-- ================== Favicons ================== -->
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />

    <!-- ================== Fonts ================== -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Dana:wght@400;500;700&display=swap"
      rel="stylesheet"
    />

    <style>
      /* ================== RESET ================== */
      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }
      body,
      html {
        margin: 0;
        padding: 0;
        font-family: "Dana", sans-serif;
        line-height: 1.6;
        scroll-behavior: smooth;
        transition: background 0.3s, color 0.3s;
      }
      img {
        max-width: 100%;
        display: block;
      }
      a {
        text-decoration: none;
        color: inherit;
      }

      /* ================== VARIABLES ================== */
      :root {
        --bg: #fff;
        --text: #0f1720;
        --muted: #6b7280;
        --brand: #ff3c00;
        --card: #f8fafc;
        --glass: rgba(15, 23, 32, 0.04);
        --radius: 14px;
        --shadow: 0 8px 30px rgba(15, 23, 32, 0.06);
      }
      body.dark {
        --bg: #0e1015;
        --text: #f8f8f8;
        --muted: #a0a0a0;
        --card: #1c1c1c;
        --glass: rgba(255, 255, 255, 0.05);
      }

      /* ================== LAYOUT ================== */
      body {
        background: var(--bg);
        color: var(--text);
      }
      .container {
        width: min(1200px, 92%);
        margin: 0 auto;
      }
      .center {
        display: flex;
        justify-content: center;
        align-items: center;
      }

      /* ================== HEADER ================== */
      header {
        position: sticky;
        top: 0;
        z-index: 60;
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 0.7),
          rgba(255, 255, 255, 0.55)
        );
        backdrop-filter: blur(6px);
        border-bottom: 1px solid rgba(15, 23, 32, 0.04);
        transition: background 0.25s;
      }
      body.dark header {
        background: linear-gradient(
          180deg,
          rgba(30, 30, 30, 0.9),
          rgba(30, 30, 30, 0.7)
        );
        border-color: rgba(255, 255, 255, 0.08);
      }
      .header-inner {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 18px 0;
      }
      .logo {
        display: flex;
        align-items: center;
        gap: 12px;
        cursor: pointer;
        color: var(--text);
      }
      nav.main-nav {
        display: flex;
        gap: 18px;
        align-items: center;
      }
      nav.main-nav a {
        font-weight: 600;
        padding: 8px 12px;
        border-radius: 10px;
        color: var(--muted);
        transition: all 0.18s;
      }
      nav.main-nav a:hover {
        color: var(--brand);
        background: var(--glass);
        transform: translateY(-2px);
      }
      .hamburger {
        display: none;
        flex-direction: column;
        gap: 6px;
        width: 36px;
        height: 24px;
        justify-content: center;
        cursor: pointer;
      }
      .hamburger span {
        display: block;
        height: 2px;
        border-radius: 2px;
        background: var(--text);
        transition: all 0.18s;
      }

      /* ================== HERO ================== */
      .hero {
        padding: 86px 0 60px;
      }
      .hero-grid {
        display: grid;
        grid-template-columns: 1fr 420px;
        gap: 40px;
        align-items: center;
      }
      .hero h1 {
        font-size: 44px;
        font-weight: 900;
        margin-bottom: 14px;
        color: var(--text);
      }
      .hero p {
        margin: 0 0 24px;
        color: var(--muted);
        max-width: 60ch;
      }
      .hero-cta {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        padding: 14px 20px;
        border-radius: 12px;
        font-weight: 800;
        background: linear-gradient(90deg, var(--brand), #ff7a4a);
        color: #fff;
      }
      .hero-right {
        border-radius: var(--radius);
        padding: 18px;
        border: 1px solid rgba(15, 23, 32, 0.08);
        box-shadow: var(--shadow);
      }
      .mini-item {
        display: flex;
        align-items: flex-start;
        gap: 12px;
        margin-bottom: 18px;
      }

      /* ================== CTA ================== */
      .cta-section {
        padding: 90px 0;
      }
      .cta-box {
        background: linear-gradient(135deg, var(--brand), #ff7a4a);
        border-radius: 20px;
        padding: 35px 51px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        color: #130f0f;
        box-shadow: 0 30px 80px rgba(255, 60, 0, 0.35);
      }
      .cta-text h2 {
        margin: 0 0 10px;
        font-size: 32px;
        font-weight: 600;
      }
      .cta-text p {
        margin: 0;
        opacity: 0.9;
        font-size: 16px;
      }
      .cta-btn {
        background: #fff;
        color: var(--brand);
        font-weight: 800;
        padding: 16px 26px;
        border-radius: 13px;
        transition: transform 0.18s ease, box-shadow 0.18s ease;
      }
      .cta-btn:hover {
        transform: translateY(-3px);
      }

      /* ================== TEAM ================== */
      .team-section {
        padding: 80px 0;
      }
      .team-section h2 {
        font-size: 32px;
        font-weight: 800;
        margin-bottom: 16px;
      }
      .team-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 46px;
        justify-items: center;
      }
      .team-member {
        border-radius: var(--radius);
        padding: 20px;
        box-shadow: var(--shadow);
        transition: transform 0.3s;
      }
      .team-member img {
        border-radius: 40%;
        width: 145px;
        height: 127px;
        margin-bottom: 10px;
        object-fit: cover;
      }
      .team-member h4 {
        margin: 8px 0 4px;
        font-size: 16px;
      }
      .team-member p {
        margin: 0;
        color: var(--muted);
        font-size: 14px;
      }

      /* ================== FOOTER ================== */
      .site-footer {
        margin-top: 50px;
        padding: 70px 0 0;
        background: #ffffff;
        color: #0f1720;
        border-top: 1px solid rgba(15, 23, 32, 0.08);
      }
      .footer-container {
        width: min(1200px, 92%);
        margin: auto;
        display: grid;
        grid-template-columns: 1.2fr 1fr 1fr;
        gap: 50px;
      }
      .footer-col h4,
      .footer-col.brand h3 {
        margin-bottom: 14px;
        color: #0f1720;
      }
      .footer-col.brand h3 {
        font-size: 26px;
        font-weight: 900;
      }
      .footer-col p {
        max-width: 42ch;
        color: #4b5563;
        line-height: 1.8;
      }
      .footer-col ul {
        list-style: none;
        padding: 0;
        margin: 0;
      }
      .footer-col li {
        margin-bottom: 10px;
        color: #374151;
        font-size: 15px;
      }
      .footer-social {
        display: flex;
        gap: 14px;
        margin-top: 24px;
      }
      .footer-social a {
        width: 46px;
        height: 46px;
        border-radius: 14px;
        display: grid;
        place-items: center;
        background: rgba(15, 23, 32, 0.04);
        transition: all 0.3s ease;
      }
      .footer-social svg {
        width: 20px;
        height: 20px;
        fill: #0f1720;
      }
      .footer-bottom {
        margin-top: 60px;
        padding: 18px 0;
        text-align: center;
        font-size: 14px;
        color: #6b7280;
        border-top: 1px solid rgba(15, 23, 32, 0.08);
      }

      /* ========== Responsive ========== */
      @media (max-width: 1000px) {
        .hero-grid {
          grid-template-columns: 1fr 320px;
        }
      }
      @media (max-width: 760px) {
        nav.main-nav {
          display: none;
        }
        .hamburger {
          display: flex;
        }
        .hero-grid {
          grid-template-columns: 1fr;
          gap: 28px;
        }
        .team-grid {
          grid-template-columns: 1fr 1fr;
        }
      }
      @media (max-width: 500px) {
        .team-grid {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>

  <body>
    <header>
      <div class="container header-inner">
        <div class="logo" onclick="scrollToTop()">
          <div class="logo-icon">
            <span class="short">TEAM<span class="dot">.</span>max</span>
            <span class="full">تیم طراحی سایت</span>
          </div>
        </div>
        <nav class="main-nav">
          <a href="#services">خدمات</a>
          <a href="#team">تیم</a>
          <a href="#contact">تماس</a>
        </nav>
        <div class="hamburger" onclick="toggleMobile()">
          <span></span><span></span><span></span>
        </div>
      </div>
      <div
        id="mobileMenu"
        style="max-height: 0; overflow: hidden; transition: max-height 0.3s"
      >
        <div
          class="container"
          style="
            display: flex;
            flex-direction: column;
            gap: 8px;
            padding: 10px 0;
          "
        >
          <a href="#services" onclick="toggleMobile()">خدمات</a>
          <a href="#team" onclick="toggleMobile()">تیم</a>
          <a href="#contact" onclick="toggleMobile()">تماس</a>
        </div>
      </div>
    </header>

    <main>
      <section class="hero container">
        <div class="hero-grid">
          <div>
            <h1>طراحی سایت حرفه‌ای برای رشد کسب‌وکار</h1>
            <p>سایت‌های سریع، امن و بهینه برای افزایش نرخ تبدیل کاربران.</p>
            <div style="display: flex; gap: 12px; margin-top: 20px">
              <a class="hero-cta" href="#contact">شروع همکاری</a>
              <button class="dark-toggle" onclick="toggleDarkMode(event)">
                تغییر تم
              </button>
            </div>
          </div>
          <aside class="hero-right"></aside>
        </div>
      </section>

      <section id="services" class="container" style="padding: 80px 0">
        <h2>خدمات ما</h2>
        <div
          style="
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 24px;
          "
        >
          <div class="hero-right"><h4>طراحی سایت شرکتی</h4></div>
          <div class="hero-right"><h4>طراحی سایت فروشگاهی</h4></div>
          <div class="hero-right"><h4>UI/UX اختصاصی</h4></div>
          <div class="hero-right"><h4>سئو و بهینه‌سازی</h4></div>
        </div>
      </section>

      <section class="cta-section">
        <div class="container cta-box">
          <div class="cta-text"><h2>آماده‌ای سایتت بفروشه؟</h2></div>
          <a href="#contact" class="cta-btn">مشاوره رایگان</a>
        </div>
      </section>

      <section id="team" class="container team-section">
        <h2>تیم ما</h2>
        <div class="team-grid">
          <div class="team-member">
            <img src="" alt="عضو تیم" />
            <h4>عضو تیم</h4>
          </div>
          <div class="team-member">
            <img src="" alt="عضو تیم" />
            <h4>عضو تیم</h4>
          </div>
        </div>
      </section>

      <footer class="site-footer">
        <div class="footer-container">
          <div class="footer-col brand">
            <h3>TEAM<span class="dot">.</span>max</h3>
          </div>
          <div class="footer-col">
            <h4>خدمات</h4>
            <ul>
              <li>طراحی سایت</li>
            </ul>
          </div>
          <div class="footer-col">
            <h4>تماس</h4>
            <ul>
              <li>📧 info@teammax.ir</li>
            </ul>
          </div>
          <div class="footer-social"></div>
        </div>
        <div class="footer-bottom">© 2025 Team Max</div>
      </footer>
    </main>

    <script>
      function toggleMobile() {
        const menu = document.getElementById("mobileMenu");
        menu.style.maxHeight = menu.style.maxHeight === "0px" ? "400px" : "0px";
      }
      function scrollToTop() {
        window.scrollTo({ top: 0, behavior: "smooth" });
      }
      function toggleDarkMode(e) {
        e.stopPropagation();
        document.body.classList.toggle("dark");
        e.currentTarget.textContent = document.body.classList.contains("dark")
          ? "روز"
          : "شب";
      }
    </script>
  </body>
</html>
